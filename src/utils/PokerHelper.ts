import { PokerPot } from "@/types/Poker";

export function editPotMaximum(pot: PokerPot, newMaximum: number): PokerPot[] {
    // Recursive function, should update all pots below this one with the new maximum and contributions
    

    return [];
}